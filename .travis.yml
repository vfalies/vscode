sudo: required

services: 
  - docker

addons:
  apt:
    packages:
      - docker-ce

before_install:
  - docker --version

  - docker build -t vfac/vscode .
  - docker build -t vfac/vscode-php .

script:
  - docker run --rm -ti vfac/vscode -v
  - docker run --rm -ti vfac/vscode-php -v